/**
 * CAPA DE DOMINIO (el centro del hexágono).
 * Contiene el negocio del restaurante en Java puro: sin Spring, sin JPA, sin HTTP.
 * Si mañana cambiamos la base de datos o el framework, este código no se toca.
 */
package com.udec.restoflow.domain;
