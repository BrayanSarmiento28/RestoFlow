/**
 * Configuración de Spring: conecta cada puerto con su adaptador (inyección de dependencias)
 * y crea el administrador inicial la primera vez que arranca el sistema.
 */
package com.udec.restoflow.infrastructure.config;
